
<h1>New Bookmark</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for Bookmarks</p>
    <ul>
      <li>Make sure the it rhymes.</li>
      <li>Don't use the letter "A".</li>
      <li>The incessant use of hashtags will get you banned.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for [@category, @bookmark] do |f| %>
    <% if @bookmark.errors.any? %>
      <div class="alert alert-block">
        <h4>There are <%= pluralize(@bookmark.errors.count, "error") %>.</h4>
        <ul>
          <% @bookmark.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
    <%= control_group_tag(@bookmark.errors[:url]) do %>
      <div class="form-group">
        <%= f.label :url%>
        <%= f.text_field :url, class: 'form-control', placeholder: "Enter bookmark url with http:// or https://" %>
      </div>
    <% end %>
      <div class="form-group">
        <%= f.submit "Save", class: 'btn btn-success' %>
      </div>
    <% end %>
  </div>
</div>